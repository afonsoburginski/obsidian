---
tags:
  - report
date: 2026-07-20
---
**Report Diário - 20/07/2026**

Dia de fechar o fix do sistema selecionado nas rotas system-scoped e replanejar a semana. O 2271 foi mergeado depois de destravar o build de produção, que quebrava por um import órfão no videowall apontando pra arquivos de storage da cameras que a própria PR removeu. Em paralelo, o 2272 (tópicos Kafka faltando no seed do deploy) está em review. Também mudei o foco da Sprint 25: o Dashboard de câmeras sai da semana e entra o backend da tela de Eventos.

**SOFTWARE-2271 - guarda de sistema selecionado nas rotas system-scoped (#885, mergeada):**
- O CI quebrava no build de produção do web-attlas: o videowall importava dois arquivos de storage da cameras que esta PR removeu ao centralizar o system-id no guard novo.
- Reapontei o videowall pro helper compartilhado (`readSelectedSystemId` / `StorageKeys` em `core/shared`), build verde de novo, e mergeei na develop.

**SOFTWARE-2272 - tópicos Kafka faltantes no seed do deploy (#888, em review):**
- Adiciona os 3 tópicos que faltavam no `kafka-init` (os 2 de federação AD do ms-audit e o stream de analítico do ms-cameras), pra um deploy limpo criá-los sozinho sem o ms-audit crashar no boot.

**Planejamento - troca de escopo da Sprint 25:**
- Dashboard de câmeras (2213-2219) voltou pro backlog; o foco da semana passa a ser o backend da tela de Eventos de câmeras (2220-2223, base de 16 pts). O 2224 é condicional e segue no backlog.

PR's mergeadas:
- `885 - fix: exige sistema selecionado nas rotas system-scoped (SOFTWARE-2271)`

PR's em andamento:
- `888 - fix: semeia tópicos Kafka faltantes no seed do deploy (SOFTWARE-2272)`
