---
tags:
  - report
date: 2026-08-17
---
```
Report Diário - 17/08/2026

Dia dividido entre revisão de pull requests de outros squads e entrega própria no videowall externo, além de um problema recorrente do ambiente local que corrigi de vez.

Painel físico de videowall (SOFTWARE-2434, SOFTWARE-2477):
- Entreguei a primeira metade do espelhamento de tela no painel físico: a plataforma passou a guardar a fonte e a camada já criadas no equipamento e reaproveitar as duas na tomada seguinte, em vez de recriar do zero a cada operador que assume o painel.
- Corrigi o texto do atalho de espelhamento, que ainda falava em projetar uma cena da plataforma quando o recurso real é espelhar a tela do próprio operador.

Câmeras Hikvision (SOFTWARE-2532):
- Adicionei tolerância a falha intermitente no monitoramento dessas câmeras, que hoje marcava a câmera como desconectada a cada falha isolada de rede, e corrigi a miniatura, que tentava ler pelo canal errado do protocolo do fabricante.

Débitos técnicos identificados (a tratar):
- No redesign do painel físico do videowall, identifiquei que o botão de liberar o espelho age direto, sem pedir confirmação, embora a regra do produto exija confirmação sempre; não corrigi agora porque é mudança de comportamento, não de camada visual.

PRs mergeadas:
- `1659 - Persistência da fonte e camada do espelho no processador do painel, parte 1 de 2 (SOFTWARE-2434)`
- `1662 - Texto do atalho de espelhamento do videowall (SOFTWARE-2477)`
- `1666 - Tolerância de falha e miniatura das câmeras Hikvision (SOFTWARE-2532)`

PRs revisadas:
- `1575 - Tópico dedicado de auditoria do organization (SOFTWARE-2276, aprovada com ajustes)`
- `1651 - Revisão de configuração de plano em execução no frame de tempo real (aprovada com ajustes)`
- `1594 - Distinção entre forçado pontual e permanente em UNE (aprovada com ajustes)`
- `1648 - Transições da estrutura UNE atuada (aprovada com ajustes)`
- `1617 - Contrato full stack de ações em planos de execução (aprovada com ajustes)`
- `1590 - Boot escopado dos testes de integração do organization (aprovada com ajustes)`

PRs em andamento:
- `1670 - Boot resiliente do consumer Kafka em quatro microsserviços quando o broker está fora do ar`
- `1678 - Redesign do painel físico de videowall com cards e aviso honesto de brilho e estado`
```
